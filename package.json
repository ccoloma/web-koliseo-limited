{
  "name": "web-koliseo-limited",
  "repository": "https://github.com/ccoloma/web-koliseo-limited",
  "devDependencies": {
    "gulp": "~3.9.1",
    "gulp-clean": "~0.3.2",
    "gulp-rename": "~1.2.2",
    "gulp-uglify": "~2.0.0",
    "gulp-concat": "~2.6.1",
    "del": "~2.2.2",
    "gulp-webserver": "~0.9.1"
  },
  "scripts": {
    "clean": "rm -rf js/",
    "compile": "gulp dist",
    "deploy": "gulp & jekyll serve --watch --baseurl '' --host 0.0.0.0"
  }
}
